name: First_Workflow

on:
    push: 
          branches:
              -main

jobs:
    job1:
          runs_on: ubuntu_latest   
          steps: 
              - name: List folders          
                run: ls -al